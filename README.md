# swift-sample-order

Order integration for the Sample domain.

The `Sample Order` product adapts `Order.Comparator` to Sample's native
comparator-driven `Batch` construction. Borrowing percentile, minimum, median,
and maximum access remain owned by the Sample atom.
